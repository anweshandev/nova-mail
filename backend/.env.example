# Environment Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Secret for authentication tokens
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Encryption Key for storing passwords (32 bytes hex, generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-super-secret-encryption-key-change-in-production-use-openssl-rand-hex-32

# PocketBase Configuration
# PocketBase is used to store users, sessions, and settings
# Download PocketBase from: https://pocketbase.io/docs/
POCKETBASE_URL=http://127.0.0.1:8090

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Note: IMAP/SMTP server settings are auto-discovered from:
# - https://autoconfig.{domain}/mail/config-v1.1.xml (Mozilla autoconfig)
# - https://autodiscover.{domain}/autodiscover/autodiscover.xml (Microsoft autodiscover)
# - Fallback to mail.{domain} if not found
